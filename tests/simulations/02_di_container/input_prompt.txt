Manager, I need a Dependency Injection container in 'di_project/container.py'. It must support singleton/factory registration and recursive resolution (via type hints/auto-wiring). Use a programmatic API (container.resolve). Crucially, it MUST detect circular dependencies and raise CircularDependencyError. I've reached full certainty, so run the loop now.